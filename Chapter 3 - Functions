#Write a function named collatz that has one parameter named number.
def collatz(number):
    #if number is even, then collatz should print number//2 and return this value
    if (number%2)==0:
        number=number//2
    else:
    #if number is odd, then collatz should print and return 3*number+1
        number = (number*3)+1
        
    return (number)
    
#write a program that lets the user type in an integer and that keeps calling
#collatz on that number until the function returns the value 1.
#remember to convert the return value from input to an integer with the int function

print('Please enter a number')
while True:
    try:
        number = int(input())
        while True:
            if number != 1:
                number = collatz(number)
                print (number)
            else:
                print ('You have reached the end of the simplest math problem.')
                break
    except:
        print('Please type in an integer')
#add try and except statements to the previous project to detect 
#whether the user types in a noninteger string.
